#include <string.h>
#include <stdio.h>

#include "funcs.h"
/*
char* formatting(char string[]){
    char newString[1024];

    if(!strncmp(string, "IAM ", 4)){
        while(strchr(string + 4, " ") != NULL){
            printf("espaco\n");
        }
    }

    return newString;
}
*/